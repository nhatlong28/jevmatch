# Jev Match visual references

These generated concepts are implementation references, not pixel-perfect assets
to ship in the application. `DESIGN.md` is authoritative when a concept contains
sample content or navigation that is outside the MVP.

- `jobs-dashboard.png`: authenticated shell, summary strip, filters, and job table.
- `create-job.png`: role details, job-description entry, and generation readiness.
- `evaluation-plan.png`: editable criterion rows and publication readiness.
- `candidate-application.png`: public application form and resume-upload state.
- `applications-ranking.png`: sortable applications table and evidence inspector.

